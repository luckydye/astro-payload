{
	"name": "astro-payload",
	"version": "1.0.0",
	"scripts": {
		"dev": "pnpm build:package && docker-compose up -d && dotenv -- pnpm --filter=web dev",
		"build:package": "dotenv -- pnpm --filter=@payload/astro build",
		"build": "dotenv -- pnpm --filter=web build",
		"serve": "dotenv -- pnpm --filter=web serve"
	},
	"engines": {
		"node": ">=18"
	},
	"dependencies": {
		"dotenv-cli": "^7.0.0"
	}
}
