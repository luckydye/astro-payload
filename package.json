{
	"name": "astro-payload",
	"version": "1.0.0",
	"scripts": {
		"dev": "docker-compose up -d && dotenv -- pnpm --filter=web dev",
		"build": "dotenv -- pnpm --filter=web build",
		"serve": "dotenv -- pnpm --filter=web serve"
	},
	"engines": {
		"node": ">=18"
	},
	"dependencies": {
		"dotenv-cli": "^7.0.0"
	}
}
