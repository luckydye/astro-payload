{
	"name": "web",
	"types": "module",
	"scripts": {
		"dev": "astro dev",
		"build": "payload build && astro build && tsup",
		"serve": "node ./dist/server/server.mjs"
	},
	"dependencies": {
		"@payload/astro": "workspace:^",
		"astro": "^2.2.0",
		"payload": "^1.6.32"
	},
	"devDependencies": {
		"tsup": "^6.5.0"
	},
	"tsup": {
		"entry": [
			"./payload.config.ts",
			"./payload.ts"
		],
		"format": [
			"esm"
		]
	}
}
