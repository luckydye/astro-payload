{
	"name": "web",
	"type": "module",
	"scripts": {
		"dev": "astro dev",
		"build": "payload build && astro build && tsup ./payload.config.ts",
		"serve": "node ./dist/server/server.mjs"
	},
	"dependencies": {
		"@payload/astro": "workspace:^",
		"astro": "^2.2.0",
		"payload": "^1.6.32"
	},
	"devDependencies": {
		"tsup": "^6.5.0"
	}
}
