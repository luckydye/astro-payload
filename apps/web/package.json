{
	"name": "web",
	"type": "module",
	"scripts": {
		"dev": "astro dev",
		"build": "payload build && astro build && tsup",
		"serve": "node ./dist/server/entry.mjs"
	},
	"dependencies": {
		"@luckydye/astro-payload": "0.0.4",
		"astro": "^2.2.0",
		"payload": "^1.6.32"
	},
	"devDependencies": {
		"tsup": "^6.5.0"
	},
	"tsup": {
		"entry": [
			"./payload.config.ts",
			"./payload.ts"
		],
		"format": [
			"esm"
		]
	}
}
